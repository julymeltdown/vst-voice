# SEAM 최종 진행도 평가 및 마무리 작업 계획 (2026-09-23)

기준 리비전: `96210cd1` (branch `codex/production-readiness-completion`, origin/master 반영 완료)
성격: 코드·테스트·실행 증거에 기반한 진행도 평가와 마무리 계획. 이 문서는 구현 승인·단위 승인·출시 승인을 새로 부여하지 않는다.
이전 문서: `SEAM_FINAL_PROGRESS_AND_FINISHING_PLAN_2026-09-22_KO.md` (그대로 보존, 이 문서가 이를 대체한다)

---

## 0. 한눈에 보는 결론

**결론부터: 엔진은 거의 다 만들어졌다. 문제는 그 엔진이 "노래를 못 한다"는 것이고, 그 원인은 이제 코드가 아니라 데이터다.**

세 개의 숫자를 절대 하나로 뭉개면 안 된다. 이 프로젝트가 한 달째 진전이 없어 보이는 이유가 정확히 여기에 있다.

| 축 | 수치 | 의미 |
|---|---|---|
| **로컬 승인 단위** | **6/48 = 12.5%** | 계획 단위 기준. "코드가 12.5%만 있다"는 뜻이 아니다 |
| **출시 승인 요구사항** | **0/20 = 0%** | 계약이 요구하는 최종 승인. 아직 하나도 없다 |
| **출시 게이트 증거** | **0.0%** | 29개 필수 승인 행(EB 9 + R 20) 전부 미충족 |
| **파이프라인(배관) 완성도** | **약 90%** | 녹음→설계→생성→편집→패키징→설치→재열기→내보내기 전 구간 동작 |
| **가창 음질 달성도** | **약 0%** | 측정으로 확인됨. 사람이 들어본 적이 아예 없다 |

핵심 문장 하나로 요약하면:

> **이 프로젝트는 "가수를 만드는 기계"는 다 만들었는데, 그 기계에 넣을 "진짜 목소리 데이터"가 없다. 그리고 그 사실은 코드를 더 고쳐서 해결되지 않는다.**

---

## 1. 이번 턴에 실제로 한 일 (실행 증거)

실행한 것만 기록한다. 실행하지 않은 것은 "통과"로 쓰지 않는다.

### 1.1 외부 도구가 SEAM 파일을 실제로 여는지 검증 (신규, 가장 중요한 성과)

지금까지 상호교환(interchange) 검증은 **SEAM이 쓴 파일을 SEAM이 다시 읽는** 자기 검증이었다. 자기 자신과 일치하는 코덱은 여전히 틀릴 수 있다

그래서 SEAM이 실제로 만든 파일을 **SEAM이 아닌 도구**로 열게 했다.

- 도구: OpenUtau 커밋 `8c0dc40`의 자체 역직렬화기 + OpenUtau가 MIDI에 쓰는 라이브러리 DryWetMidi 7.2.0
- 대상: 고정 코퍼스 프로젝트 `tests/singing_quality/corpus/original-melody.seam`에서 프로덕션 CLI로 내보낸 실파일

| 검사 | 결과 |
|---|---|
| USTX 역직렬화 | **통과** — 80개 음표, tone·position·UTF-8 가사 전부 일치 |
| USTX 재직렬화 왕복 | **통과** — OpenUtau 자체 직렬화기로 다시 읽힘 |
| SMF 파싱 | **통과** — 80개 음표 + 80개 lyric 메타 이벤트, 위치 정확 |
| 손실 보고 | **정확** — USTX는 손실 2건을 소스 경로와 함께 보고, SMF는 0건(잃을 것이 없으므로) |

**과정에서 내 실수를 하나 잡았다.** 처음에 "MIDI에 가사가 없다(`midiTextEvents=0`)"는 결과가 나왔는데, 이건 제품 결함이 아니라 **내 검증 도구의 잘못**이었다. DryWetMidi는 텍스트 메타 이벤트를 기본 ASCII로 해석하므로, 파일에 멀쩡히 들어 있는 UTF-8 `あ`를 `???`로 보여준 것이었다. 파일 원본 바이트를 확인하니 `e3 81 82`로 정확했다. 도구를 UTF-8로 고치고, 테스트는 **원시 바이트를 검사**하도록 만들었다.

**교훈:** 검증 도구 자체가 틀릴 수 있다. "실패"가 나오면 제품을 의심하기 전에 계측기를 먼저 의심해야 한다. 그리고 이번 건은 그 반대 방향의 위험도 보여준다 — 계측기를 의심하지 않고 그대로 보고했다면 **없는 결함을 만들어내고, 있는 결함을 놓칠 뻔했다.**

### 1.2 이번 턴에 추가한 것

| 항목 | 내용 |
|---|---|
| `export-score` CLI 명령 | 저장된 프로젝트를 에디터와 **동일한 변환 서비스**로 USTX/MIDI 내보내기. 연구용 스크립트가 아닌 프로덕션 경로 |
| `tests/test_score_export_interop.py` | CI에서 돌아가는 회귀 테스트. 위치·길이·음높이·UTF-8 가사·손실 집합을 바이트 수준으로 고정 |
| `tools/openutau_oracle/` | 외부 검증기 하네스 (README에 OpenUtau 커밋 고정, 재현 절차 포함). CI에는 넣지 않음 — .NET과 OpenUtau는 이 저장소의 의존성이 아님 |

**외부 검증기를 CI에 넣지 않은 이유:** 의존성이 움직이면 조용히 안 돌아가는 검증기는 아예 없는 것보다 나쁘다. 대신 CI가 돌릴 수 있는 절반(바이트 계약)은 테스트로 고정했다.

### 1.3 전체 회귀

| 검사 | 결과 |
|---|---|
| Release 전체 스위트 연속 실행 | **183/183 통과** (총 6회: -j 6 ×4, -j 12 ×1, 직렬 ×1) |
| 외부 Beta 계약 / phase12b 계약 | **통과** |
| 소스 폐쇄성(source closure) | **통과** |
| **CI 5개 잡 전부** (`b22adec7`) | **전부 success** — windows-helper, native-platform-matrix ×3(macOS/Ubuntu/Windows), isolated-release-candidate |

전체 스위트 1회차에서 `seam_phase12b_tests`가 한 번 `Subprocess aborted`로 실패했다. 이후 재현을 시도한 결과:

| 시도 | 결과 |
|---|---|
| 단독 실행 (직렬) | 200회 연속 통과 |
| 24개 동시 실행 | 24/24 통과 (exit 0) |
| 전체 스위트 `-j 6` / `-j 12` | 각각 183/183 통과 |

**중요한 단서:** 이 테스트는 실패 시 `std::cerr`로 진단을 출력하지 않고 **종료 코드만 반환**하는 구조다(`tests/test_phase12b.cpp` — 예: `return 9`). 그런데 실패는 `return`이 아니라 `Subprocess aborted`(SIGABRT)였다. 즉 실패 지점은 검사 로직이 아니라 **그 이후**이며, 프로세스 종료(teardown) 과정에서 발생한 것으로 보인다.

종료 경로는 이미 정리(new)를 하고 있다: `EditorRuntime::~EditorRuntime` → 콜백 해제 후 `shutdown()` → `AuthoringRuntime::shutdown()` → `previewWorker_.request_stop()` + `join()`, 그리고 `AuthoringRenderCoordinator::shutdown()` → `worker_.request_stop()` + `join()`. 구조적으로는 맞다. 다만 **렌더 워커가 진행 중일 때 종료가 겹치면** 콜백 경로가 정리 도중의 상태를 건드릴 여지가 남아 있다.

**결론: 재현 실패. 원인 미특정.** 전체 스위트가 부하를 받는 특정 타이밍에서만 나오는 것으로 보이며, 1회 관측이다. A4로 남긴다.

---

## 2. 진행도: 영역별 정확한 수치

### 2.1 계획 단위 (48개)

| 상태 | 개수 | 단위 |
|---|---|---|
| **승인됨 (ACCEPTED)** | **6** | U1, U2, U3, U4, U5, U29(로컬 POSIX 한정) |
| 구현됨·미승인 | 10 | U6, U7, U10, U11, U17, U18, U23, U24, U30, U31 |
| 부분 (PARTIAL) | 28 | U8, U9, U12, U13, U14, U16, U19–U22, U25–U28, U32–U41, U44–U47 |
| **착수 안 됨** | **4** | **U15, U42, U43, U48** |

합계 6 + 10 + 28 + 4 = 48. 네 줄이 정확히 48개 단위를 중복 없이 덮는다.

승인 6개는 전부 **로컬 구현 승인**이며, 출시 승인이 아니다.

### 2.2 출시 게이트 (이게 진짜 문제)

기계가 판정하는 GO 조건은 `tools/external_beta/release_gate.py:177` 한 줄이다:

```python
return GateResult("EXTERNAL_BETA_READY", not errors and not blocked, tuple(errors), blocked)
```

현재 이 함수에 들어가는 입력은 **전부 0 또는 비어 있다**:

| 게이트 입력 | 현재 상태 |
|---|---|
| EB-001~EB-009 PASS | **0/9** |
| 증거 레코드 | **0개** (최소 20개 필요) |
| R1~R20 충족 | **0/20** |
| 계약 사례 PASS | **0/83** |
| 실증 기준 해결 | **0/11** (175개 임계 셀 미설정) |
| 출시 승인 | **0건** |
| `releasedResources` | `[]` |
| 리소스 매트릭스 | `UNRESOLVED` |
| 평가 프로필 | `UNRESOLVED` |

**중요:** 계약의 프로필·매트릭스가 `UNRESOLVED`인 한, 후보(candidate) 품질과 무관하게 **READY에 도달하는 것이 구조적으로 불가능**하다.

게이트 도구 자체는 완성되어 있다(검증기 정의 오류 0건). **기계는 완성됐고 증거가 0이다.**

### 2.3 남은 격차의 성격 분류 — 여기가 한 달 지연의 진짜 원인

| 분류 | 개수 | 내용 |
|---|---|---|
| 코드 작업만으로 가능 | 4 | 후보 봉투 조립, 보고서 구조, 상태 필드 |
| **사람 증거가 필수** | **7** | 임계값 결정, 권리 서류, 독립 리뷰어, 창작자 5명, 코호트 세션, 역할 승인 |
| **외부 의존** | **5** | 불변 아카이브, macOS 서명·공증, **Windows**, 9개 호스트 튜플(DAW 라이선스), 서명된 전작 |
| 혼합 | 3 | 동결 결정, 증거 레코드, 보고서 내용 |

16개 격차 중 **12개가 에이전트 혼자서는 못 닫는다.** 이게 "한 달째 돌려도 안 끝나는" 이유다. 남은 일은 개발이 아니라 **연결과 승인**이다.

---

## 3. 가장 중요한 발견: 가창 음질의 벽은 데이터다

이번에 심층 확인한 내용이다. 이게 이 프로젝트의 본질이다.

### 3.1 실제로 존재하는 모델

| 항목 | 사실 |
|---|---|
| 학습된 acoustic 모델 | **있음** — DiffSinger 계열 DDPM, 128 hidden, 6 layer |
| 학습된 vocoder | **있음** — mini-NSF-512 |
| 저장소 내 체크포인트 | **0개** (전부 `/Users/lhs/seam-corpus-pauses-2026-09-19-r1/` 외부, 6.1 GB) |
| 학습 데이터 | **424 WAV / 약 41분 / 레시피 1개 / 목소리 1개** |

**결정적 사실:** 그 41분은 사람 목소리가 아니다.

> "Every WAV was rendered by this repository's own procedural singer through the production export path; no third-party recording is present."
> — `corpus-evidence.txt`

즉 **학생 모델이 선생 모델(자기네 절차적 가수)을 따라 학습했다.** 학생은 선생을 넘을 수 없고, 선생의 음질도 아무도 들어본 적이 없다. 41분은 경쟁 모델들이 쓰는 데이터의 약 **1/1000** 수준이다.

### 3.2 측정된 음질 (전부 실패)

| 지표 | 값 | 목표 | 판정 |
|---|---|---|---|
| 무성음 RMS 비율 | **0.657** | [0.8, 1.2] | **실패** |
| 무성음 flatness | 0.0406 | 기준 0.5863 | **14배 과도하게 유성적** |
| USTX 손실 | — | — | (참고: 정상) |

**가장 무서운 숫자:** 학습 epoch를 늘렸더니(e37) 손실은 내려갔는데 **실제 적용 시 피치 오차가 35센트 → 342센트로 폭발**했다. 즉 **학습 목표와 실제 샘플러가 어긋나 있다.** 이 상태에서 더 학습시키면 더 나빠진다.

### 3.3 구조적 원인 (코드로 확인됨)

`tools/voice_model_training/uv_noise_excitation.py`에 명시되어 있다:

- 내보낸 vocoder의 `noise_sigma`가 **0으로 고정**되어 있다
- 즉 **무성음 자음에 비주기(잡음) 에너지가 아예 공급되지 않는다**
- 무성음 자음이 14배 과도하게 유성적인 것은 **학습 실패가 아니라 설계상 당연한 결과**다
- 이미 `uv-gated-v1`(잡음 주입, 진폭 1/3)이 구현되어 있다. 측정치는 **제한적이다**: 서명된 무성음 잔차가 0.226 → 0.203으로 줄었지만, 이 증분 0.024는 컨트롤 잔차의 약 10%이자 periodicity 목표 효과(0.239)의 약 1/10이다. 게다가 곡별로는 악화되는 항목이 있다(procedural-song-00024 피치 27.66 → 33.30센트, 무성음 `|rmsRatio-1|` 0.172 → 0.189). 문서 스스로 "복제되거나 통계적으로 확립된 이득은 주장하지 않는다"고 명시한다

**이건 이번 주에 고칠 수 있는 코드 작업이다.**

### 3.4 사람이 들어본 적이 없다

- 46개 블라인드 청취 패킷: **조립 완료, 점수표는 머리글 + 빈 46행**
- 모든 산출물에 `listening: NOT_REVIEWED`, `singerQualified: false`, `releaseEligible: false`
- 39.5초 일본어 곡: "**No person has performed this session. No listener has judged this song.**"

프로젝트가 "품질"이라 부르는 것은 전부 합성 기준음 대비 DSP 측정이다. **아무도 실제로 들어본 적이 없다.**

---

## 4. 마무리 작업 계획

우선순위는 "싸고 확실한 것 → 비싸고 불확실한 것" 순이다.

### 즉시 (이번 주, 코드 작업, 에이전트 가능)

| # | 작업 | 근거 | 비용 |
|---|---|---|---|
| **A1** | **무성음 여기(excitation)를 프로덕션 기본값으로 승격** | 현재 잡음 공급이 문자 그대로 0이다. `uv-gated-v1` 이미 구현됨. 단 **측정 이득이 작고 곡별 악화가 있으므로, 승격 전에 무성음 RMS 가드레일(0.657 vs [0.8, 1.2])이 실제로 개선되는지 먼저 확인**해야 한다 | 낮음 |
| **A2** | **학습 목표 ↔ 배포 샘플러 불일치 수정** | e37 회귀가 증명함. 안 고치면 다음 학습도 똑같이 망가진다 | 중간 |
| **A3** | U30 승인 감사 (U29 때와 동일한 형태) | 게이트가 요구하는 형식 | 낮음 |
| **A4** | `seam_phase12b_tests` 간헐 실패 원인 규명 | 3회 반복에서는 재현 안 됨. CI 신뢰도 문제 | 낮음 |

### 곧 (1~2주)

| # | 작업 | 내용 |
|---|---|---|
| B1 | **A1·A2 적용 후 재학습 1회** | 현재 코퍼스로 가능한 최선. 단, 천장은 선생 모델 품질 |
| B2 | 스튜디오 발행 경로 해시 미검증 상태 복구 | 실패한 녹음이 갇히면 앱에서 탈출 불가 |
| B3 | 네이티브 가져오기 다이얼로그 실제 조작 검증 | Return/Escape/포커스 미검증 |
| B4 | 실제 마이크 권한·분리 동작 검증 | 주입 테스트는 하드웨어 증거가 아님 |

### 반드시 사람이 해야 함 (에이전트 불가)

| # | 작업 | 왜 |
|---|---|---|
| **C1** | **46개 블라인드 청취 패킷 채점** | **이미 다 준비돼 있다. 돈도 코딩도 안 든다. 이거 하나가 아래 전부를 바꾼다** |
| C2 | 권리 정리된 **사람 가창 코퍼스 확보** (최소 10시간) | 진짜 천장. 법적 결정이기도 함 |
| C3 | 임계값 11개 동결 | 측정 결과로만 정할 수 있음 |
| C4 | 독립 리뷰어 (음악가·보컬 프로듀서·원어민·창작자 5명) | 계약이 요구 |

### 외부 의존 (에이전트 불가)

| # | 작업 |
|---|---|
| D1 | macOS 서명·공증 (서명 키 정책 결정 필요) |
| D2 | **Windows 런타임** — README에 TODO로 명시됨, macOS만 보유 |
| D3 | 9개 호스트 튜플 (Logic/REAPER/Bitwig 등, DAW 라이선스 필요) |
| D4 | 불변 외부 아카이브 |

### 순서에 대한 판단

**C1을 먼저 해야 한다.** A1·A2를 고쳐도 그 결과가 좋아졌는지 판단할 기준이 지금 없다. 46개 패킷은 이미 조립·해시 고정·봉인 키까지 끝나 있고, 점수표는 빈 채로 기다리고 있다. **이 프로젝트에서 가장 싸고 가장 정보량이 많은 단일 행동이다.**

C2(사람 코퍼스)는 **돈과 시간이 드는 진짜 결정**이다. 코드를 아무리 고쳐도 이걸 안 하면 천장은 선생 모델 품질에 묶인다. 지금 바로 결정해야 할 사항으로 올린다.

---

## 5. 리스크

| 리스크 | 내용 | 대응 |
|---|---|---|
| **최대 리스크** | 사람 코퍼스 없이 코드만 계속 고치면 **또 한 달이 그대로 간다** | C2를 결정 사항으로 에스컬레이션 |
| 세션 유실 | 사용자가 이전에 작업 유실을 겪음 | 이번 턴 전부 커밋·푸시 완료 |
| 간헐 테스트 실패 | `seam_phase12b_tests` 1회 실패, 재현 안 됨 | A4로 원인 규명 필요 |
| Windows | README에 TODO로 명시. 컴파일은 되나 런타임 미검증 | macOS 우선 진행 유지 |
| 학습 회귀 | epoch 증가가 성능을 악화시킴(e37) | A2 없이 재학습 금지 |

---

## 6. 시간 추정

24/7 에이전트 작업 기준. **"에이전트가 할 수 있는 부분"만**의 추정이다.

| 구간 | 예상 | 전제 |
|---|---|---|
| 즉시 작업 (A1~A4) | 1~2일 | 코드만 |
| B1 재학습 1회 | 2~4일 | CPU 학습, 천장 낮음 |
| B2~B4 | 3~5일 | 네이티브 검증 포함 |
| **소계 (에이전트 가능)** | **약 1~2주** | 여기서 코드는 사실상 끝난다 |
| C1 청취 채점 | 사람 시간 **2~3시간** | 즉시 가능 |
| C2 사람 코퍼스 확보 | **수 주 + 비용 + 법적 결정** | 여기서 프로젝트 운명이 갈린다 |
| C2 이후 재학습 | GPU 수일 | M3 Max CPU로는 불가 |
| C3~C4, D1~D4 | 수 주 | 사람·외부 |

**정직한 결론: 에이전트가 코드로 줄일 수 있는 시간은 약 1~2주 남았고, 그 이후는 전부 사람·데이터·법률·외부 도구다.**

"한 달째 돌렸는데 왜 안 끝나나"에 대한 답: **지난 한 달은 대부분 에이전트가 할 수 있는 일(파이프라인·검증기·게이트)에 쓰였고, 그건 거의 다 끝났다. 남은 건 에이전트가 할 수 없는 일인데, 그 사실이 명시적으로 결정되지 않은 채 코드 작업이 계속 이어져 왔다.**

---

## 7. 지금 당장 결정해야 할 것

1. **46개 청취 패킷을 누가 언제 채점할 것인가** — 비용 0, 정보량 최대
2. **사람 가창 코퍼스를 확보할 것인가** — 확보하지 않으면 천장이 고정된다
3. **Windows를 Beta 범위에서 계속 제외할 것인가** — README에 이미 TODO로 명시됨

---

## 8. 이 문서가 주장하지 않는 것

- 구현 승인·단위 승인·출시 승인을 새로 부여하지 않는다
- 6/48은 "코드가 12.5%"가 아니다
- 90% 파이프라인 수치는 배관 완성도이며 제품 가치가 아니다
- 외부 검증은 "읽을 수 있음"을 증명할 뿐, **같은 소리가 난다거나 음악적으로 쓸 만하다는 뜻이 아니다**
- 실제 DAW 교환과 청취 판정은 여전히 미검증이다
