#include "seam/platform/audio_callback.hpp"

// Intentionally empty: Phase 1 audio types are header-only so the real-time
// callback can be inlined and audited before the native iPlug2 adapter lands.
