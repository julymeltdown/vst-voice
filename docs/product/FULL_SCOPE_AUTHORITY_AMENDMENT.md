# Full-scope Beta authority amendment

Authority: the user-settled full-scope decision in report revision 2,
`VIRTUAL_SINGER_FEASIBILITY_AND_CODE_ROADMAP_2026-09-05.md`, SHA-256
`635606cfd10be803612dfcb47cf84651796a06860ff34dc8343705eac20c9c01`,
implemented through U2 of the 2026-09-05 full-scope plan.

Every R1–R20 and V01–V18 outcome is mandatory before Beta GO. Japanese-first
is execution order; Japanese, English and Korean, classical and qualified
neural rendering, original procedural female voice design, real and generated
source routes, all section-7 expressions, native USTX and SMF, character
performance, both desktop targets and all nine host tuples remain required.

This decision supersedes the earlier creator-plan scope-ratification veto,
Japanese-only release scope, MIDI deferral, limited expression-selector
scope, preservation of defective duplication/slur behavior, and restriction
against amending External Beta validators. The earlier study keeps its
historical NOT_RUN/PENDING results and `schema8Authorization: false`; those
values record an unexecuted study and do not veto the later authorized work.
No participant session or approval is created by this amendment.

The older production plan's unfinished U60 and release-support obligations
remain mandatory. Existing human-source permissions, data-only bank rules,
privacy, signing, installation, host and archive controls remain applicable.
New source/resource contracts must distinguish human, procedural and neural
provenance honestly; U14/U45 own the later resource-validator integration.

The acceptance contract embeds the SHA-256 of the actual full-product
contract bytes. The existing `acceptanceContractSha256` and candidate-root
commitment therefore bind the complete registry and evaluation basis.
Changing bytes requires a new digest and invalidates old candidate evidence.
A path-only reference or a rebound outer digest with stale inner content
cannot satisfy the gate.

U2 defines 83 typed cases, not acceptance results. Each case declares applicable
language/resource/backend/platform/host dimensions, an executed workload,
typed raw artifacts and independent review roles. `all-released` and
`each-declared-language` are expansion obligations over the final frozen
resource matrix, never wildcard PASS values. Every advertised supported
combination requires evidence; a documented incompatibility must leave a
tested supported combination for each mandatory feature.

The versioned `protocolCatalog` and `checkCatalog` are the machine-readable
owners of nonnumeric acceptance semantics. They require counterbalanced
unfamiliar tasks, distinct producer/reviewer identities, complete saved-song
and producer journeys, and rejection of successful bounce with pending,
failed, stale or missing required vocals. Workload `description` text is
explanatory; replacing prose cannot alter the canonical checks, operations
or constraints. Every case requires its exact versioned check IDs and typed
raw operation evidence. U45 must validate those artifacts before acceptance.

Eight existing melody/melisma, vibrato and language-context cases also require
`fp.check.chunk-continuity.v1`. It compares a whole phrase with forced splits
inside sustain and phonetic context, binds phoneme count/timing, F0 and
modulation-phase observations, and requires an adjacent-context mutation with
the resulting dependency/cache invalidation. The original 83 cases and all
R/V outcomes remain intact; this makes the existing U7 obligation explicit.

The fixed floors remain 60 phrases per language, three complete songs across
the languages, five independent pre-GO creators, median pitch error at most
30 cents, 90% of designated steady frames within 50 cents, and a 30 ms timing
edit accurate within one rounding sample. The 500 ms p95 classical small-edit
budget and older 150 ms median/400 ms p95 two-second preview workload are
separate obligations. Their measurements must retain octave errors, voiced
coverage, declared exclusions, dry vocals and exact environment identities.

Eleven empirical criteria and the actual resource matrix remain UNRESOLVED.
Their named U19/U20/U35/U36 and related owners must bind measured budgets,
reference machines and independently reviewed musical rubrics before final
acceptance. Changing failed thresholds requires a reviewed contract revision
and fresh affected evidence. Freezing a definition cannot approve an output.

The empirical specifications define 175 exact result cells. Expression
tolerances cover 13 controls with intended-property, neutral, pitch and timing
bounds. Generation and neural latency cover both platforms, warm/cold state
and named workloads. Resource, cancellation, source-volume, language/listener,
reproducibility and machine-profile grids retain their own dimensions, units
and comparators. Each resolved cell binds machine, workload, resource matrix,
backend revision, provider and precision identities. These are typed result
requirements; all canonical values remain null until the named owners provide
measured, independently reviewed qualification. A scalar or omitted cell
cannot resolve a multi-dimension criterion.

The U45 semantic validator is not yet available. The central READY evaluator
emits an explicit EB-009 error and blocked ID even if every row claims PASS;
CLOSED inherits that error. No manual-ready flag or production fixture bypass
is provided. U45/U46 must validate raw content, restored identity and all
promotion paths before removing this blocker. JSON Schema proves shape only.

Pre-GO independent creator/producer acceptance precedes READY. The subsequent
external cohort, CLOSED decision and later public activation remain distinct
events; none is fabricated or made a circular prerequisite to starting Beta.
