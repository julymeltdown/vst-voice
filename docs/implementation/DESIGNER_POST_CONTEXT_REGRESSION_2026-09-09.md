# Accumulated Designer changes: full local regression

## Verdict

119 of 120 registered Release CTest entries passed in 129.21 seconds with `-j 2`.
The run exited 8, not success. The only failure was `seam_tracked_source_closure`:
505 required inputs existed locally but were not in the Git index. No missing
input records were emitted. Its policy checks source-tree and CMake/workflow
requirements against `git ls-files --cached`; it does not establish that indexed
bytes equal a committed, published or releasable tree.

No source gate was weakened and no files were staged merely to make it green.
The dirty worktree remains based on HEAD
`741ae2f244b9d3ff8eb6f31dc1f73cae54ea974d`. The new result supersedes older full-suite
snapshots for the current accumulated code changes; it is not proof of external
voice quality, Windows runtime, all installed host tuples or Beta GO readiness.

Command: `ctest --test-dir build/release --output-on-failure -j 2
--output-log build/release/current-full-regression.log`.
Retained output: `evidence/designer-post-context-full-2026-09-09/ctest.log`.
The 505 count describes the run before this report and its copied log were added.

## Source-creation live follow-up

In the rebuilt Studio at 720x520, creating `s / neutral` immediately selected and
highlighted NOISE CENTER, scrolled it into view and exposed Src/CV/VC actions.
Creating `k / neutral` through the painted Add stop button then selected BURST
CENTER at control 21/29, accounting for the preceding frication rows. Both
observations required no manual parameter-page navigation.

The attempted keyboard-edit follow-up returned UI-tool error
`keyNotFound("right")`. No keyboard event was delivered and no alternative input
mechanism was used. Therefore auto-scroll/visual selection is verified for these
two cases, but immediate native keyboard focus is not qualified by this run.
No saved recipe or bank was modified; all drafts were temporary QA state.

The prior QA process exited normally after discarding its temporary draft, with
microphone unopened and zero recorded frames. The rebuilt QA instance remains
open for further checks. All repository changes are preserved and uncommitted.
