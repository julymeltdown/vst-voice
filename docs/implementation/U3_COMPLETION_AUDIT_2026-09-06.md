# U3 acceptance audit — 2026-09-06

**Verdict: U3 implementation acceptance PASS.** The observed default-render style defect is repaired and verified. This is completion of the plan's U3 canonical vocabulary/migration/persistence unit in the current uncommitted worktree, not publication to `origin/master`, U4 completion or Beta GO.

Authority: `docs/plans/2026-09-05-1718-feat-full-scope-beta-go-plan.md`, U3. U4 owns revisioned pronunciation compilation/correspondence; later musical-quality and take-admission units remain in the full plan and are not silently folded into this milestone.

| Explicit U3 requirement | Evidence and outcome |
| --- | --- |
| Canonical ownership, pronunciation identity, proposed/accepted take state, style/resource intent, vibrato and typed dynamics | Domain performance/project types, schema-8 helpers, region-state/contract/snapshot tests. Implemented storage and bounded validation; acoustic consumers remain governed by their downstream units. |
| Genuine schemas 1–7 migrate without invented measurements or musical loss | Historical codecs emitted new schema-1/4/5/6/7 fixtures; historical Phase 2/3 demo output supplies schemas 2/3. `test_schema8_performance.cpp` decodes each, checks neutral additions and re-encodes/reloads. New schema-1/4 checks assert note pitch/time/lyric and schema-4 timing lock/routing. See `tests/fixtures/projects/generators/README.md` for exact commits, seeds, drivers and byte hashes. The fixtures are newly generated by old writers, not claimed to have been saved years ago. |
| Unavailable legacy styles stay unresolved; exact bank only | `test_style_migration.cpp` and `test_plugin_style_migration.cpp` use actual installed signed-bank fixtures and cover missing identity, project opening, recovery, initialization, relinking and plugin replacement. No arbitrary installed-bank substitution. |
| Coupled ownership/hint/style/expression edits undo and round-trip together through project and plugin | New `U3 coupled ownership hint style and expression roundtrip through project and plugin` case in `test_region_performance_state.cpp`. One command changes all four categories, then verifies actual file save/load and plugin encode/decode/runtime replacement before undo, after undo and after redo, with exact project equality. |
| Invalid/future inputs fail without overwriting the original file | New `U3 rejected opens preserve source bytes and the current document` case drives `ProjectLifecycleService::open` with future schema, negative vibrato depth and overflowing numeric input. It verifies failure, unchanged live project/revision/base hash and byte-identical source. Existing numeric/codec tests cover the broader value/range matrix. |
| Downstream use of defined intent and explicit algorithm/cache identity | Runtime pronunciation identity is source-bound and included in snapshot content identity, and relevant performance state is frozen. Default snapshots now consume `VocalTrack::styleSelection`; whole-project rendering delegates to that resolution. Missing saved styles are rejected without substitute units, unselected multi-style banks require choice, unresolved legacy intent requires exact-bank migration, and sole-style defaulting is explicit. A nonempty render API style remains a deliberate temporary override, without changing saved intent. Two new snapshot/project-render tests cover these semantics and cache identity. Audible expression algorithms remain downstream units, not a U3 qualification claim. |

## Verification

- Current full strict Debug and Release builds passed; no warning-as-error setting was disabled. Existing duplicate-library linker warnings remain.
- All 15 selected acceptance CTest entries passed in both configurations: contract, live job context, region state, schema-8, style migration, plugin migration, coupled commands, snapshots, edit preservation, existing U2/U3/standalone suites, core, and both source contracts. Debug elapsed 89.49 seconds; Release 16.88 seconds.
- Later historical schema-5/6/7 fixture additions were rechecked through the rebuilt schema-8 test target in both configurations; they do not change production code.
- The historical schema-4 demo's unrelated old WAV warning did not get bypassed: the fixture driver linked only its unchanged, successfully built codec/domain/core libraries. The other historical writer builds and self-round-trips succeeded. No historical source was patched.
- `git diff --check` passed. Source publication/index closure is not asserted: these changes remain uncommitted.

Final post-style-fix verification: full strict Debug and Release builds passed, followed by all 15 acceptance CTest entries in both configurations (Debug 87.78 seconds; Release 15.12 seconds). This rerun supersedes the earlier pre-fix timings above. The new project-render test was corrected to assert the existing whole-project failure contract when every track fails, including the preserved style error in its context; no production failure was suppressed. The fixture now includes a genuine second declared style/unit so style-change tests verify valid rendering rather than assuming an unavailable style should render.

## Next implementation action

Current continuation recheck: rebuilt and passed the selected schema migration, coupled performance commands, region persistence, live-result context, pronunciation reconciliation and phoneme timing suites in Debug and Release, together with producer/export integration (8/8 CTest entries per configuration; 4.94/2.99 seconds). This is focused regression evidence for the current worktree, not a repetition of the full original acceptance matrix. U3 stays accepted; U4/U5 acceptance is retained and U6 remains active.

Continuation status: the U4 action below was the handoff at U3 acceptance, not the current backlog. U4 and U5 have since passed local implementation acceptance; see their acceptance reviews and `FULL_SCOPE_BETA_EXECUTION.md`. U6 is now the next unaccepted unit, with U7 and procedural-engine prerequisites in progress. U3 does not need to be restarted.

Continue U4: stable historical token/edit bindings, complete shared pronunciation consumption and remaining pronunciation-changing paths. Existing resolver/reconciliation implementations carry forward; do not restart them. U6/U8/U38 and all other full-scope units remain required for audible performance, backend capabilities and generated-take admission. No release-ready singer, host certification or Beta GO is claimed.
