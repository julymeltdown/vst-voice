# Source quality execution evidence

`ctest-full.log` and `ctest-full-cases.log`: 119/120 full run, 321.17 s,
before final shared-readiness/query hardening. The only failure is source closure.

`ctest-focused.log` and `ctest-focused-cases.log`: latest five-entry run,
25.76 s, including 826 core cases and four actual CLI journeys.

These are engineering fixtures and do not contain real singer qualification.
