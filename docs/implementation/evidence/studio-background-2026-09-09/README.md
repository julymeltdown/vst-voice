# Studio selection evidence

`ctest-focused.log` and `ctest-cases.log` preserve the current seven-entry
execution, not the earlier full-suite result. No real singer, native Windows,
or measured latency qualification is implied.
