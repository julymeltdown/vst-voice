# SEAM 최종 진행도 및 마무리 계획 (2026-09-22)

기준 리비전: `f2d3b150` (기준 `ed41968d` + 검증·커밋·푸시 완료된 네이티브 입력/상호교환 통합 배치)
성격: 코드·테스트·계약 실행 증거에 기반한 진행도 평가와 마무리 작업 계획. 이 문서는 구현 승인, 단위 승인, 출시 승인을 새로 부여하지 않는다.
이전 점검 문서: `SEAM_PROGRESS_AND_FINISHING_PLAN_2026-09-22_KO.md` (20:11 KST 시점 기록, 그대로 보존)

---

## 0. 한눈에 보는 결론

**내부 엔진은 상당히 구현되어 있지만, 제품 완성도는 "출시 승인" 기준으로는 아직 초기 단계다. 이 둘을 같은 숫자로 뭉개면 판단을 잘못한다.**

- **로컬 승인 단위: 6/48 = 12.5%** (U1–U5 + 로컬 POSIX 한정 U29). 이는 "코드가 12.5%만 존재한다"는 뜻이 아니다.
- **출시 요구사항 승인: 0/20 (0%)**, 계약 사례 83개 전부 `beforeBetaGO`, 미확정 프로필/매트릭스 항목 13개, 등록된 출시 리소스 0개.
- **코드 구현량 기준 전체 %는 산출하지 않는다.** 단위별 크기와 부분 구현량을 측정한 자료가 없다. 50%·80%·95% 같은 숫자는 근거 없는 추정이다.
- **가장 큰 단일 격차는 "실제로 쓸 만한 가수 한 명"이 승인되지 않았다는 점**이다. 39.5초 일본어 곡은 실제로 렌더링되지만 산출물 스스로 `releaseEligible=false`, `listening=NOT_REVIEWED`, `creatorWorkflow=NOT_OBSERVED`로 표시한다.
- **남은 일은 새 엔진 개발이 아니라 연결·승인이다**: 현재 배치 마감 → 한 사람의 목소리로 한 곡 완주 → 품질·언어·리소스 승인 → 설치·출시 승인.

---

## 1. 이번 턴에 새로 확인한 사실 (실행 증거)

이번 점검에서 실제로 실행한 검사만 기록한다. 실행하지 않은 것은 "통과"로 쓰지 않는다.

| 검사 | 결과 | 의미와 한계 |
|---|---|---|
| Release 6개 상호교환/입력 대상 | **6/6 통과, 145개 케이스, 0.69초** | recording_input 11, conversion_review 6, smf 18, ustx 34, interchange_service 17, u2 59. 미커밋 배치의 회귀 증거 |
| Debug 동일 6개 대상 | **6/6 통과, 8.89초** | 두 구성 모두 통과. 다만 위 6개 대상 범위 한정 |
| Release 가수/설계/설명 대상 | **4/4 통과, 23.47초** | voice_designer, voice_design, original_singer_song_journey, formant_expression. 기존 곡 여정이 아직 살아 있음 |
| 전체 제품 계약 Python 검사 | **44개 통과, 21.9초** | 검증기 정의·게이트 동작 증거이며 제품 합격 증거가 아님 |
| 계약 실시간 판정 | 요구사항 20, 사례 83, 정의 오류 0, 미확정 13, 출시 리소스 0 | `matrixStatus=UNRESOLVED`, `evaluationProfile.status=UNRESOLVED` |
| 추적 소스 완전성 | 이전 실패(미등록 7개) → **`SOURCE_CLOSURE=PASS`** | 파일 유실이 아니라 통합 전 미등록 상태였음 |
| 원격 `origin/master` | **`f2d3b150`** (푸시마다 `ls-remote`로 확인) | `ed41968d → 162adc60 → 2a797b96 → c077cf5d → 27e49aee → f2d3b150` |
| Release 전체 스위트 | 181개 중 **176개 통과 / 5개 실패** | 실패 5개는 `phase12b_contract`, `macos_source_contract`(이번에 수정), 미빌드 실행 파일 2개, `inventory_preflight`(재빌드 후 통과). 아래에서 원인 분리 |

### 이번에 실제로 고친 결함 (모두 커밋·푸시 완료)

1. **USTX 회귀 1건 (테스트 결함)**: `tests/test_ustx_interchange.cpp`의 낡은 단언이 "빈 배열도 손실"이라고 기대했다. 계약대로 **빈 메타데이터는 손실 0, 값이 있는 메타데이터는 경로별 손실 보고**로 단언을 교체했다. 단순 삭제가 아니라 양성/음성 대조를 함께 유지했다. (162adc6)
2. **녹음 대기 상태의 복구 경로 부재 (실제 결함)**: `RecordingInputSession::discardPending()`을 추가해 메모리 캡처만 폐기하고 이미 저장된 WAV는 사용자 데이터로 보존한다. Studio에서 `X`가 폐기를 수행하고, 복구 재추출은 1회로 제한하며 두 번째 시도는 실행 가능한 안내 메시지로 거부한다. 회귀 테스트가 폐기 후 파일 바이트 동일성, 무효 폐기/승인 거부, 새 테이크 시작을 검증한다. (2a797b9)
3. **macOS 소스 계약 드리프트 (테스트 결함)**: `test_voicebank_studio_exposes_a_fail_closed_recording_probe`가 Studio 내부 `recording_.clear()`를 찾고 있었다. 수명주기가 세션 API로 이동했으므로 계약을 실제 소유자로 옮기고, 실패한 내보내기가 승인보다 먼저 반환하며 승인/폐기 둘 다 pending을 요구한다는 단언을 추가했다. (c077cf5)
4. **Release/Debug 양 구성 동시 통과**를 확인해 미커밋 배치를 커밋·푸시할 수 있는 상태로 만들었다.

### 이번에 고치지 않고 남긴 것 (소스/실행 수준으로 확인)

- **전체 스위트의 기존 실패 (이번 변경과 무관함을 확인)**:
  - `seam_inventory_preflight_tests`: 로컬 실패는 **오래된 `seam_voicebank_cli` 바이너리 때문**이었다. CLI와 테스트를 다시 빌드하니 통과한다. 이 실패는 회귀가 아니라 빌드 트리 드리프트다. **전체 스위트 결과를 읽을 때 이 구분이 중요하다.**
  - `seam_contextual_unit_selection_tests`, `seam_sample_microscope_tests`: 실행 파일이 없어 `Not Run`이었고 개별 빌드로 생성된다. 빌드 트리 드리프트.
  - ~~`seam_phase12b_contract`~~ **해결 완료**: 실패 원인은 미구현이 아니라 **낡은 계약**이었다. 이전 리팩터가 마이크로스코프 상태·레이아웃·히트테스트를 공용 컨트롤러로 옮겼는데 계약은 CLAP 런타임이 직접 만들던 옛 구조를 검사하고 있었다. 계약을 실제 소유자(공용 컨트롤러 + CLAP의 위임)로 옮기고 검증을 강화했다. `verify_phase12b_contracts.py` 6개 섹션 통과, `phase12b_contract`/`clap_microscope_tests`/`sample_microscope_tests`/`contextual_unit_selection_tests` Release 통과. (f2d3b15)
  - CI `ed41968d` 실행의 macOS 작업은 그 외에도 `onnx`/`torch` 미설치로 인한 `seam_voice_model_training_tests` 실패가 있었다. 이는 환경 의존 실패이며 코드 회귀가 아니다.

### 교훈: "실패"를 세 종류로 나눠야 한다

이번 점검에서 전체 스위트 실패 5건 중 실제 결함은 1건(낡은 계약)뿐이었다.

1. **빌드 트리 드리프트** (2건): 실행 파일이 없거나 오래된 바이너리를 참조. 재빌드로 해소. **CI는 이 경우 통과**하므로 로컬 실패가 곧 회귀는 아니다.
2. **환경 의존** (CI 1건): `onnx`/`torch` 미설치.
3. **실제 결함** (1건): 계약이 코드 구조 변경을 따라가지 못함. 동작은 정상이었고 계약이 틀렸다.

앞으로 전체 스위트 결과를 보고할 때 이 분류를 먼저 적용한다.

### Windows 전용 컴파일 실패를 발견해 수리

CI의 Windows 작업 두 개가 모두 `seam_synthesis` 빌드에서 실패하고 있었다(`error C2672`, `std::construct_at`). 원인은 `libs/seam-synthesis/src/unit_selection.cpp`의 지역 `State` 구조체가 **감싸는 함수의 지역 변수 `none`을 기본 멤버 초기화자에서 참조**한 것이었다. Clang은 허용하지만 MSVC는 `std::vector`가 원소를 값 초기화할 때 이를 거부한다. 리터럴 상수로 바꿔 이식 가능하게 수리했다. (769615b)

이 결함은 **macOS만으로는 절대 보이지 않는다.** Windows가 계약 플랫폼인 이유를 보여주는 실제 사례이며, README의 "Windows TODO" 항목에 새 근거로 추가할 만하다.

**중요한 경계:** 이 수리는 **빌드 컴파일 실패**를 고친 것이다. Windows 런타임 검증(설치, 호스트, 오디오 장치)은 여전히 하드웨어가 필요하며 이번 범위가 아니다. Windows를 TODO로 두는 결정은 바뀌지 않는다.

**검증:** CI `windows-helper-process` 작업이 `769615b`에서 처음으로 통과했다(11분 30초). 이전에는 같은 파일에서 실패했다.

### Linux(GCC) 전용 컴파일 실패도 수리

같은 방식으로 Ubuntu 작업이 `tests/test_sample_microscope_details.cpp`에서 `-Werror=range-loop-construct`로 실패하고 있었다. `for (const auto size : {std::pair{...}})`가 매 반복 `std::pair`를 복사하는데 GCC가 이를 오류로 처리한다. 참조 바인딩으로 수리했다. (da665d1)

### 두 수리의 공통 교훈

**macOS(Clang)만으로 개발하면 Windows(MSVC)와 Linux(GCC) 전용 컴파일 실패를 볼 수 없다.** 이번에 발견한 2건이 정확히 그 사례이며, 둘 다 **코드 로직이 아니라 이식성** 문제였다. 로컬 전체 스위트가 초록이어도 CI 3개 플랫폼 작업은 별도로 확인해야 한다.
- **아직 남은 것 (이번 배치 범위 밖)**:
- **네이티브 모달 실제 조작 미검증**: AppKit 검토 대화상자 코드는 존재하지만 Return/Escape, 포커스 복원, 긴 텍스트 스크롤, 작은 화면은 실행 검증이 없다(이전 시도는 Mac 잠금으로 실패).
- **실제 마이크 장치·권한·분리 동작 미검증**: 주입된 장치 팩토리 테스트는 하드웨어 증거가 아니다.
- **CLAP 임베디드 편집기 상호교환 미구현**, **실제 OpenUtau/DAW 교환 미검증**.

---

## 2. 진행도 수치와 정확한 의미

| 지표 | 값 | 정확한 의미 |
|---|---:|---|
| 로컬 승인 단위 | **6/48 = 12.5%** | U1–U5(`FULL_SCOPE_BETA_EXECUTION.md:1321`)와 로컬 POSIX 한정 U29(`INTEGRATED_SINGER_EXECUTION.md:415–422`). 모든 플랫폼 승인이 아님 |
| 그 외 단위 | 42/48 미승인 | 상당수는 부분 구현 완료. "처음부터 개발"이 아님 |
| 출시 요구사항 승인 | **0/20 = 0%** | 요구사항 20개, 사례 83개 전부 미승인. 코드 구현률 0%라는 뜻이 아님 |
| 출시 등록 리소스 | **0개** | 개발용 레시피·뱅크·모델은 존재. 계약 표는 비어 있음 |
| 미확정 평가 항목 | **13개** | 음향 경계, 표현 허용오차, 발음 점수, 정체성 루브릭, 생성/신경망 예산, 기준 장비, 소스 볼륨, 재현 허용오차, 프로필 동결, 리소스 매트릭스 |
| 코드 구현량/남은 노동량 % | **산출 불가** | 측정 자료 없음. 위 두 숫자와 혼동 금지 |

이전 세션에서 기록된 "7/48", "55–60%" 주장은 철회된 상태다(`INTEGRATED_SINGER_EXECUTION.md:424–427`). 이번 문서는 그 철회를 유지한다.

---

## 3. 영역별: 있는 것 / 남은 것

| 영역 | 이미 있는 것 | 끝내야 하는 것 |
|---|---|---|
| 녹음 없는 목소리 설계 | 발성·공명·자음 계열·스타일 레시피, Designer 편집·미리듣기·저장·원자적 게시 | 목표 여성 캐릭터 정체성, 자음 명료도·음역·스타일 승인, UI에서 뱅크·곡까지 무중단 완주 |
| 실제 녹음/외부 음원 | 입력 수명주기, 캡처 검증, 뱅크 제작·검사·재녹음·패키징·설치 서비스 | 물리 마이크 승인/분리/복구 실제 검증, 대기 녹음 복구 동작, 실제 입력 리소스 승인 |
| 곡 편집·출력 | 약 39.5초 일본어 곡, 튜닝, undo/redo, 저장·재열기, 설치 가수 렌더, 최종 WAV | 처음 보는 가사 무도움 완주(창작자 관찰), 음악적 품질 승인 |
| 클래식/절차적 표현 | breathiness·tension·growl·formant, 소유권·캐시·출력 경계 | 가수×렌더러×언어×표현 지원 표 확정과 음악적 유효성 승인 |
| 신경망 가수 | 학습·데이터·ONNX·네이티브 워커·렌더 경로 | 무성 자음의 약함/주기성, 학습 외 자료 성능, 실제 모델 배포·실행 예산. 현재 기준 음성은 unvoiced RMS 0.657 vs [0.8,1.2]로 자체 가드레일 실패 |
| 일본어/영어/한국어 | 일본어 읽기 검증, 영어 강세·음절, 한국어 분해·문맥 규칙 | 영어 26개 부트스트랩 사전 확장, 한국어 사전 의존 예외, 실제 가수의 언어별 발음 승인 |
| 자동 표현·테이크·화음 | 제안·부분 수락·취소·undo·화음 기반 | 생성기 4채널 밖 필수 기능, UI 비교/재생성, 실제 창작 효용 |
| 캐릭터 | 입 모양 에셋과 렌더 큐 연동 | 출시용 자산·정체성·권리 증거, 최종 UI 사용성 |
| 출시/검증 | 계약·증거 검증기·복구·패키징 기반 | 실제 리소스 등록, 프로필 동결, 서명·설치·호스트 실행 증거 |
| 플랫폼 | macOS 개발·검증 가능 | **Windows는 TODO로 보류**(README 명시). 단, 전체 Beta GO 계약은 두 플랫폼을 요구하므로 Windows 보류 상태에서 Beta GO 선언 불가 |

---

## 4. 마무리 작업 계획 (W1–W4)

기존 R1–R20/U1–U48을 바꾸지 않는다. 아래는 실행 순서이며 새 진행률 분모가 아니다.

### W1. 현재 통합 배치 마감 (가장 짧고 확실한 수확)

**목표:** 미커밋이던 네이티브 입력/상호교환 배치를 검증·승인된 상태로 만든다.

1. USTX 낡은 단언 교체 (완료: 이번 커밋)
2. Release/Debug 6개 대상 통과 (완료: 145케이스 × 2구성)
3. 녹음 대기 복구 경로 추가: 저장된 WAV 보존 + 새 목적지 재추출 또는 확인된 폐기. 강제 종료 없이 빠져나올 수 있어야 하며, 변경된 바이트를 조용히 재결합하면 안 된다.
4. 실제 macOS 실행 증거: Import → 손실 검토 → 취소/수락 → 새 미저장 문서 → Save As → 재열기. 원본 해시 보존, Return/Escape, 긴 Unicode, 마지막 행, 작은 화면, 키보드·포커스.
5. 실제 마이크: 권한 거절·장치 열기 실패·캡처 중 분리가 성공 녹음으로 게시되지 않음.
6. 독립 리뷰(제2 개발자 세션) + 통합 커밋.

**경계:** embedded editor 연결, 실제 OpenUtau/DAW 교환, Windows 실행은 이 묶음에서 끝나지 않는다.

### W2. 목소리 하나로 곡 하나 완주 (제품 가치가 보이는 첫 마일스톤)

**범위:** `libs/seam-voice-design/`, `libs/seam-voicebank-production/`, 네이티브 Designer/Studio, 기존 `tests/test_procedural_install_journey.cpp`, `tests/test_original_singer_song_journey.cpp` 확장.

1. 빈 작업 공간 → 목소리 생성·조형 → 발성/공명/자음/스타일 편집 → 저장·재열기 → 미리듣기.
2. 생성 또는 실제 녹음 → 테이크 편집·재생성 → 기존 수동 작업 보존 → 검사·검토 → 후보 패키지 → 설치.
3. 설치 가수를 선택해 처음 보는 가사로 30–60초 곡 → pitch/phoneme timing/vibrato/timbre 수정 → 저장·재열기 → 최종 WAV.
4. 취소·오래된 비동기 결과·실패 복구·원본 불변성을 같은 흐름에서 확인.
5. JSON 수작업 없이 네이티브 UI만으로 수행하고, 그 기록을 fixture 테스트와 분리해 남긴다.

**완료 조건:** 위 여정의 프로젝트/레시피/뱅크/곡/오디오 식별자가 남고, 재현 가능한 자동 회귀가 있으며, **엔지니어링 완료와 지각적 품질 승인을 분리**해 기록한다.

### W3. 실제 가수·언어·표현 품질 승인

- 절차적/클래식: 음역·음소·스타일·렌더러·지원 표현 표 확정, 자음·전이·정체성 개선.
- 다국어: 영어 사전 확장, 한국어 사전 의존 예외, 일본어 원어민 검증.
- 신경망: **한 번에 하나의 국소 실험**만. 동결된 데이터·기준·가드레일·중단 예산을 먼저 선언하고, 학습 손실이 아니라 실제 최종 PCM과 학습 외 자료로 판정한다. 현재 46개 블라인드 비교 패킷은 사람 청취 대기 상태다.
- 계약: 리소스 매트릭스와 평가 프로필 동결, 언어별 60문장·3곡·독립 창작자 5명 프로토콜.

**경계:** 사용 가능한 절차적 가수가 생겨도 신경망 요구는 면제되지 않는다(EB-009).

### W4. 설치 제품·출시 승인

- U44–U48: 지원/개인정보, 의미 증거, 후보 승격 도구, 설치·서명·호스트 여정.
- macOS 우선: standalone/CLAP/VST3/AUv2 실제 호스트, 서명·공증·설치.
- 리소스 권리·정체성 검사, 보존 아카이브 감사, 독립 승인.
- Windows는 TODO로 유지하되, 전체 Beta GO가 두 플랫폼을 요구한다는 사실은 README에 그대로 남긴다.

---

## 5. 검증·병렬화 전략

- **에이전트 검증**: 구현 에이전트와 분리된 읽기 전용 감사 에이전트를 붙인다. 이번에도 제2 개발자 세션이 계획 적합성과 누락을 지적했고, 그 지적이 그대로 작업 항목이 됐다.
- **오픈소스 대조**: OpenUtau USTX/피치·포르타멘토 규칙을 독립 오라클로 사용한다(현재 테스트에 OpenUtau 83e02c7e 수식 번역이 이미 들어 있다). 로컬 왕복만으로 상호운용을 주장하지 않는다.
- **사람이 필요한 게이트**: 청취(음질·정체성·발음), 무도움 창작자 관찰, 원어민 발음 검증, 외부 서명/호스트. 이 네 가지는 에이전트로 대체할 수 없다.
- **자동화로 대체 가능한 것**: 회귀, 계약 검증, 손실 보고 정확성, 결정론·해시, 교환 형식 정합성, 실패 주입.

### 검증 명령 (이번에 실제 통과한 것)

```sh
cmake --build build/release --target seam_recording_input_tests seam_conversion_review_tests seam_u2_tests \
  seam_interchange_service_tests seam_ustx_interchange_tests seam_smf_interchange_tests -j4
ctest --test-dir build/release --output-on-failure \
  -R '^seam_(recording_input|conversion_review|u2|interchange_service|ustx_interchange|smf_interchange)_tests$'
ctest --test-dir build/debug --output-on-failure \
  -R '^seam_(recording_input|conversion_review|u2|interchange_service|ustx_interchange|smf_interchange)_tests$'
ctest --test-dir build/release --output-on-failure \
  -R '^seam_(voice_design|voice_designer|original_singer_song_journey|formant_expression)_tests$'
python3 -B -m unittest discover -s tests/external_beta -p 'test_full_product*.py'
python3 -B scripts/verify_tracked_source_closure.py --root .
```

---

## 6. 시간 추정 (근거와 함께)

측정 자료가 아니라 현재 속도 기준 추정이다. 사람 게이트에서 대기하는 시간은 에이전트가 줄일 수 없다.

| 묶음 | 에이전트 연속 작업 기준 | 사람/외부 의존 |
|---|---|---|
| W1 마감 | 1–2 작업일 | 실제 앱 조작·마이크·독립 리뷰 세션 |
| W2 목소리→곡 완주 | 1–2주 | 무도움 창작자 관찰 1회 |
| W3 품질·언어·리소스 | 3–8주 (훈련·청취 반복에 좌우) | 청취 패널, 원어민 검증, 학습 자원 |
| W4 설치·출시 승인 | 2–4주 | 서명 인증서, 호스트 라이선스, 외부 검토 |

W3의 폭이 가장 넓고 불확실하다. 신경망 품질이 한 번의 국소 수리로 해결된다는 보장이 없기 때문이다.

---

## 7. 리스크와 판단 경계

- 세션 저장 오류로 수정이 유실된 전례가 있으므로 **검증된 배치는 즉시 커밋·푸시**한다(이번 커밋이 그 조치).
- 테스트 통과를 품질 승인으로 바꾸지 않는다. `tests/test_neural_production_render.cpp`는 환경 입력이 없으면 조기 반환하므로, 해당 PASS를 실제 모델 추론 증거로 쓰면 안 된다.
- 개발용 리소스에 청취 승인이나 출시 권한을 부여하지 않는다.
- 에이전트 수를 늘리는 것으로 품질 게이트를 대체할 수 없다. 병렬화는 코드 소유권이 분리된 작업(W1/W2/W4의 코드 부분)에만 쓴다.

---

## 8. 지금 즉시 실행할 항목

1. 이번 배치 커밋·푸시 및 소스 완전성 재통과 확인 (완료 조건: `SOURCE_CLOSURE=PASS`).
2. 녹음 대기 복구 동작 구현 + 회귀 테스트 (W1-3).
3. 네이티브 Import/Export 실제 조작 기록 (W1-4, Mac 잠금 해제 필요).
4. 설치된 절차적 가수로 W2-3 여정을 자동 회귀에 연결.
5. 46개 블라인드 비교 패킷 청취 결과 회수 또는 "확보 불가" 명시 (W3 최우선 외부 입력).
